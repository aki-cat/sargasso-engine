
SOURCE_CXX_FILES = Glob("src/main.cc")

# StaticLibrary(
#   "glfw",
#   Glob(
#     "third_party/glfw/src/*.c",
#     "third_party/glfw/src/*.h"))

Program(
  "bin/coffee-engine",
  SOURCE_CXX_FILES,
  # LIBS=["glfw"],
  # LIBPATH="./third_party/glfw/lib",
  FLAGS=["Wall", "Werror"])
